# Configuration n8n (copier vers .env — .env est gitignored)
N8N_BASE_URL=https://n8n.mon-vps.tld
N8N_API_KEY=eyJ...            # clef API REST publique n8n (header X-N8N-API-KEY)
N8N_WEBHOOK_PATH=largus-collect   # path du webhook du workflow de collecte
