[project]
name = "oscaria-ml"
version = "0.1.0"
description = "OscarIA — pilier Prix (ML)"
requires-python = ">=3.12"
dependencies = [
    "pandas>=2.2",
    "numpy>=1.26",
    "matplotlib>=3.8",
    "seaborn>=0.13",
    "scikit-learn>=1.4",
    "jupyterlab>=4.0",
    "ipykernel>=6.29",
]

[tool.uv]
package = false
