# Template des secrets locaux — copier en `.env` et remplir (le vrai `.env` est gitignored).
# Token Hugging Face pour télécharger les datasets (ex. CarDD).
# Générer sur https://huggingface.co/settings/tokens (scope read suffit).
HF_TOKEN=
