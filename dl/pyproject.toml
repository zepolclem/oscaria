[project]
name = "oscaria-dl"
version = "0.1.0"
description = "OscarIA — pilier Plaques : détection + floutage (DL vision)"
requires-python = ">=3.12"
dependencies = [
    "torch>=2.2",
    "torchvision>=0.17",
    "numpy>=1.26",
    "pandas>=2.2",
    "matplotlib>=3.8",
    "scikit-learn>=1.4",
    "pillow>=10.0",
    "jupyterlab>=4.0",
    "ipykernel>=6.29",
    "defusedxml>=0.7.1",
]

[tool.uv]
package = false
